[build]
builder = "nixpacks"

[deploy]
startCommand = "pip3 install -r requirements.txt && python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3A
