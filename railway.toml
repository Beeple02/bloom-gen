[build]
builder = "nixpacks"

[build.nixpacks]
installCmd = "python3 -m ensurepip && python3 -m pip install -r requirements.txt"

[deploy]
startCommand = "python3 main.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
